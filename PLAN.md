# Plan

## Delivered
- [x] App scaffold + UI system foundation
- [x] Supabase Postgres/Drizzle schema + migrations
- [x] Discogs source ingestion (label/artist -> release -> track)
- [x] YouTube candidate search, scoring, and manual override support
- [x] Queue orchestration + persistent mini-player
- [x] Todo/wishlist actions + recommendation seed/event capture
- [x] CSV/JSON export + legal outbound links
- [x] Core pages: Sources, Listening Station, Library, Recommendations, Settings
- [x] Release-level fallback when per-track matches are weak
- [x] Recommendation ranking from listening history + label affinity
- [x] OAuth-based YouTube playlist export from Library
- [x] Auth/onboarding flow refresh (`/welcome`, logged-in how-to-use access)
- [x] Mobile/iOS playback hardening + single-tab playback ownership guard
- [x] Production rollout on Railway with current feature set
- [x] Beginner-friendly batch controls on Sources (queue/retry/resume/pause quick actions)
- [x] Discogs link reliability hardening (slug/plural/query path normalization + UI/ingest canonicalization)
- [x] Client playback/queue reliability pass (shared event channels + enqueue/quota helpers)
- [x] Failure Center remediation audit feedback (action/scope/affected summaries + quick triage chips)
- [x] Mobile commute UX pass in Listening Station (sticky quick rail, commute preset, focused row navigation, auto-next playback toggle, skip+play action)
- [x] Failure Center phase 2 completion (category-specific remediations, scoped confirmations, richer audit feedback)
- [x] Observability phase 2 completion (breakdowns, window comparison, anomaly hints, compact dashboard summaries)
- [x] Regression coverage phase 2 completion (sources-next/remediation helpers, queue transition effect planning, OAuth callback parity)
- [x] Copy/mobile polish pass completion (Failure Center, sync status, Sources, Library, Recommendations, history/review tightening)
- [x] Reliability guardrails completion (smoke checks, explicit auth-boundary responses, live stall/failure alerts)
- [x] Product quality upgrades completion (state-aware onboarding guidance across Welcome, Settings, dashboard, empty states, and guide)

## Current Focus (P0)
- [x] Release-readiness sweep
  - Goal: lock down the final deploy + live verification path and remaining duplicate-user/data-integrity invariants so release handoff is predictable.
  - Remaining work:
    - Optionally run authenticated smoke probes with `SMOKE_COOKIE` on the target deploy when a fresh session cookie is available.
  - Completed in this pass:
    - Consolidated duplicate track todo mutation flows so API routes, server actions, and single-track toggles share the same listened/saved mutation helpers.
    - Consolidated release wishlist mutation logic so the API route and server action path share the same local/external sync behavior and result shape.
    - Reduced release-identity branching in recommendation feedback by routing it through shared release-target helpers.
    - Consolidated source bootstrap flow so dashboard source-add and release-to-source import share the same best-effort metadata refresh and immediate processing kick.
    - Consolidated release materialization so source page ingest, Discogs want import, and release-to-source creation share the same release persist + source-mapping helper.
    - Consolidated `/api/sources/next` client polling logic so the sync daemon and source status card share the same fetch/parsing and source-view priority helpers.
    - Consolidated `/api/queue/list` client loading so Listening Station and the mini-player share the same queue-list fetch/parsing helpers.
    - Consolidated `/api/queue/next` client calls so the mini-player uses one shared queue-advance/fetch helper instead of multiple inline request paths.
    - Consolidated wishlist-sync client calls so the status poller and saved-to-Discogs action share the same status/auto-sync/submission helpers.
    - Consolidated compact playback controls so play/queue buttons share the same YouTube-quota subscription and enqueue-then-play helper instead of repeating it across multiple components.
    - Consolidated operational label actions so queue processor and activation controls share the same label-status, label-active, and worker-process client helpers.
    - Consolidated settings actions so API key testing and YouTube playlist export share a single client request/response helper layer.
    - Consolidated release metadata/link lookups so the mini-player and release-link finder share the same Discogs release and finder client fetch helpers.
    - Consolidated mini-player release inspection view shaping so expanded playback now uses one shared release-inspector view helper instead of recomputing artwork, artist/label metadata, pricing rows, and outbound links inline.
    - Consolidated release external-store/search link generation so the finder service, release page, and mini-player release inspector now share one canonical release-link helper instead of hand-building Discogs/Bandcamp/Juno/Hardwax/Phonica URLs separately.
    - Consolidated positive-ID normalization so queue scope, track todo, release listened/wishlist state, source-id parsing, and client library event parsing now share one helper instead of each reimplementing dedupe-plus-positive filtering.
    - Consolidated wishlist sync release grouping so sync planning and local sync helpers now share one external-Discogs release grouping path instead of each rebuilding local-release-to-external-release mappings inline.
    - Consolidated source display-name derivation so source intake, source upsert fallback naming, and the dashboard source view now share one Discogs-slug/name helper instead of parsing label/artist URLs separately.
    - Consolidated source-kind normalization so the dashboard, source identity helpers, remediation, single-source refresh, and processing paths now share one `toSourceKind`/`getSourceKindLabel` layer instead of repeating artist-vs-label ternaries inline.
    - Consolidated source sync telemetry context so `processSingleReleaseForSource` now shares one source meta payload across loading, matching, queued, retry, complete, and error telemetry writes instead of rebuilding the same source-kind/source-name tuple on each branch.
    - Consolidated source sync telemetry writing so `processSingleReleaseForSource` now uses one local telemetry writer for per-phase writes instead of repeating the same `writeSyncTelemetry` envelope with `updatedAt` and source metadata on each branch.
    - Consolidated release-level sync telemetry shaping so `processSingleReleaseForSource` now uses shared release/track telemetry writers instead of rebuilding `releaseId`/`releaseTitle` and track progress payloads across processing, matching, and queued branches.
    - Consolidated source processing status updates so `processSingleReleaseForSource` now uses one local source-state updater for processing, complete, retry, and error transitions instead of repeating `labels` update payloads inline.
    - Consolidated client release-wishlist event state updates so Listening Station, Recommendations, and the mini-player now share one affected-release mapping/filter helper instead of each rebuilding `Set(releaseIds)` and applying wishlist updates separately.
    - Consolidated client track-todo state shaping so Listening Station and the shared player-current state now share one track-todo updater instead of each reapplying saved/listened field changes inline.
    - Consolidated Listening Station row-state mutations so play-now, player-current sync, todo updates, saved filtering, and bulk/current row actions now share one client row-state helper instead of repeating the same listened/saved/up-next row rewrites inline.
    - Consolidated Listening Station release-row mutations so optimistic wishlist toggles, wishlist event sync, confirmed/rolled-back wishlist updates, and add-source activation now share one release-row state helper instead of mixing generic release mappers with inline row rewrites.
    - Consolidated mini-player action controls so compact menus and desktop action rows now share one save-track/release-wishlist control layer instead of carrying parallel button logic for loading, labels, styling, and disabled-state rules.
    - Consolidated release-page item controls so track todo, record wishlist, and choose-match buttons now share one fixed-key action runner and one release-item button wrapper instead of carrying parallel pending/button wiring across three separate components.
    - Consolidated single-source button controls so retry, refresh, delete, and activate now share one source-action button wrapper and one button-hook return shape instead of carrying four parallel pending/run button layers.
    - Consolidated wishlist-sync action buttons so manual sync and saved-to-Discogs sync now share one wishlist-sync button wrapper and one hook return shape instead of carrying parallel pending/message/button layouts.
    - Consolidated batch source-action buttons so sync-control and remediation now share one batch-action button wrapper and one hook return shape instead of carrying parallel pending/message/button shells.
    - Consolidated account/settings action buttons so sign-out, integration disconnect, and API key test now share one account-action button wrapper and one hook return shape instead of carrying parallel pending/result/button wiring.
    - Consolidated the remaining settings export action so YouTube playlist export now uses the same account-action button/result layer instead of keeping a separate pending/error/result shell beside the other settings actions.
    - Consolidated auth form UI so login, register, password reset request, and password update now share one auth-form input/message/submit layer instead of carrying repeated field styling and success/error/submit shells.
    - Consolidated auth form payload/action handling so login, register, password reset request, and password update now share form-data payload builders and a consistent `{ pending, error, result?, run }` hook contract instead of hand-parsing `FormData` and mixing `submit`/`result` shapes per form.
    - Consolidated compact playback buttons so track queue, play-match, and recently-played replay controls now share one playback-action button wrapper and one fixed playback-button hook layer instead of each re-wrapping keyed pending/quota/request handling inline.
    - Consolidated recommendation playback controls so in-library recommendation rows now use the shared playback-button hook/wrapper layer and a dedicated row component instead of keeping panel-local queue/play wiring and feedback state inline.
    - Consolidated recommendation row rendering so in-library and outside-library recommendation cards now share one recommendation-card shell, while outside-library rows also moved their add-source/want/dismiss mutation flow into a dedicated row component instead of keeping a second panel-local implementation inline.
    - Consolidated recommendation row mutation buttons so recommendation action hooks now expose per-action pending/message state and both row types use a shared recommendation-action button wrapper instead of mixing raw buttons with parallel keyed mutation shells.
    - Consolidated segmented filter/view controls so dashboard source-state tabs, recommendation view tabs, and recently-played filters now share one segmented-control button/link component instead of each re-creating active/inactive tab styling inline.
    - Consolidated compact media rows so recently played items and the mini-player queue overlay now share one media-action row shell for artwork, title/meta text, and trailing actions instead of each rebuilding the same small media layout inline.
    - Consolidated external-link result rows so the dashboard’s Bandcamp wishlist items and the release inspector’s Bandcamp candidate list now share one bordered external-link row component instead of maintaining separate title/meta link-card implementations.
    - Consolidated the remaining dashboard library-view tabs so Library/History/Reviewed/Needs Review now use the shared segmented-control link component instead of keeping another inline copy of the same active/inactive tab styling.
    - Consolidated Discogs outbound links so dashboard source cards, recommendations, recently played, Listening Station, and mini-player release links now share one Discogs link component instead of hand-building icon-button, inline-icon, and text-button anchors per surface.
    - Consolidated mutation action buttons so account/source/library/playback/wishlist controls now use preset-driven `MutationActionButton` usage instead of routing through seven near-identical wrapper components.
    - Consolidated external outbound action links so queue/search, playlist export, label/release detail pages, mini-player, release inspector, and text-style Discogs links now share one external link component instead of hand-building button, pill, icon-circle, and text-link anchors per surface.
    - Consolidated async action feedback hooks so source retry/refresh/delete/remediation and wishlist sync actions now share one `useAsyncFeedbackAction` layer instead of each rebuilding `useAsyncAction` plus local `error/message` state handling.
    - Consolidated button-shaped mutation hooks so release-item and source buttons now import their canonical library/source hook modules directly instead of routing through three thin wrapper-hook files for the same pending/error/run contracts.
    - Consolidated async task action hooks so auth/account redirects, account task actions, source control/activation, and source intake now share one `useAsyncTaskAction` layer instead of each rebuilding `useAsyncTask` result/error plumbing inline.
    - Consolidated library action notices so Listening Station, the mini-player, and recommendation mutations now share one notice/message helper layer instead of hand-building review/save/wishlist/add-label copy per surface.
    - Consolidated library mutation fallback/error notices so core library hooks, recommendation actions, Listening Station add-label flow, and low-level library client mutations now share one notice/error helper layer instead of repeating add-label, dismiss, review, save, and wishlist copy with drift.
    - Consolidated library control labels so release-item controls, Listening Station row actions, mini-player library controls, and recommendation action buttons now share one library-action label helper instead of repeating save/wishlist/dismiss/review/add-source wording across surfaces.
    - Consolidated inbox-query finishing so the saved/reviewed/wishlisted/played query variants in `lib/queries.ts` now share one playable-row hydration, queue-state enrichment, dedupe, and active-label loading path instead of repeating the same row-enrichment epilogue across each query branch.
    - Consolidated inbox-query row selection so the saved/reviewed/wishlisted query variants in `lib/queries.ts` now share one inbox row selection contract instead of repeating the same track/release/label/match select object across six primary/fallback branches.
    - Consolidated the remaining played/wishlisted query helpers in `lib/queries.ts` so played-track ID extraction and wishlisted-record row selection now share one helper path instead of being repeated between the primary and fallback query branches.
    - Consolidated inbox-query legacy fallback scaffolding so the to-listen, wishlist, played-reviewed, and wishlisted-record loaders now share one source-release-to-legacy-label fallback wrapper instead of each carrying nested `try/catch` fallback flow and empty-result handling inline.
    - Consolidated dashboard label metadata shaping so the primary and legacy dashboard query paths now share one count-map and label-progress summary helper instead of each rebuilding loaded/fetched/track counts and `summaryText` fields inline.
    - Consolidated label-detail loading in `lib/queries.ts` so source-backed and legacy label detail now share release/progress helper loaders plus the same fallback wrapper instead of carrying duplicated release-list and progress-count branches inline.
    - Consolidated dashboard result shaping in `lib/queries.ts` so the primary dashboard path, legacy label fallback, and empty fallback now share one metrics/result builder instead of repeating the same queue/metrics/recommendations response envelope inline.
    - Consolidated dashboard metric count queries in `lib/queries.ts` so queue, track, release, and low-confidence counts now use shared source-backed count helpers instead of repeating the same source-release-to-label joins across `getDashboardData()`.
    - Consolidated dashboard label-progress loading in `lib/queries.ts` so release thumbs plus loaded/fetched/track counts now come from one shared source-backed label-progress helper instead of four inline `sourceReleases` queries inside `getDashboardData()`.
    - Consolidated inbox row loading in `lib/queries.ts` so source-backed and release-label-backed inbox loaders now share playable-where, join/select, and played-track-id helpers, and removed the dead duplicate fallback wrapper from `getWishlistData()`.
    - Consolidated the Listening Station mobile filter shell so the mobile details/summary wrapper for presets, queue filters, and bulk actions now lives in one dedicated component instead of keeping another breakpoint-specific control flow inline in `listen-inbox-client.tsx`.
    - Consolidated the Listening Station desktop filter shell so the desktop queue-filter row and bulk-action bar now live in one dedicated wrapper component instead of keeping another breakpoint-specific shell inline in `listen-inbox-client.tsx`.
    - Consolidated the Listening Station top control shell so wishlist-source controls, breakpoint-specific filter shells, and saved/wishlisted helper copy now flow through one dedicated wrapper component instead of being stitched together inline in `listen-inbox-client.tsx`.
    - Consolidated the Listening Station row summary so artwork, artist/title/meta, wishlist chip, and status badges now flow through one dedicated row-summary component instead of keeping another compact/full row-body branch inline in `listen-inbox-client.tsx`.
    - Consolidated the Listening Station row action shell so mobile queue actions and desktop trailing actions now flow through one dedicated row-action component instead of keeping two breakpoint-specific action layouts inline in `listen-inbox-client.tsx`.
    - Consolidated the Listening Station row container so selection, active/playing row chrome, row-summary, and row-action composition now flow through one dedicated row wrapper instead of being assembled inline inside the inbox list map.
    - Consolidated the Listening Station list shell so row rendering, row-state wiring, and empty-state handling now flow through one dedicated list component instead of keeping the final list-map and empty branch inline in `listen-inbox-client.tsx`.
    - Removed the dead duplicate Listening Station mobile filter wrapper so the active mobile filter flow only exists in `listen-inbox-mobile-filter-shell.tsx` instead of lingering beside an unused older copy.
    - Consolidated Listening Station control contracts so the client, header shell, control shell, queue filters, mobile filter shell, desktop filter shell, wishlist-source filter, and empty-state shell now share one inbox control/type layer instead of repeating the same filter/count unions and empty-state card shape across the component stack.
    - Consolidated Listening Station playback affordance state so inbox rows and the mobile current-track rail now share one playability/disabled-reason/search-hint helper instead of each re-deriving the same queue/play fallback logic inline.
    - Consolidated Listening Station current-track controls so the mobile quick rail and current-action grid now share one current-row contract plus track-save label helper instead of carrying a second local current-row type and manual save-label strings.
    - Consolidated track-save control labels so Listening Station rows/current controls, bulk save actions, recommendations, release track controls, and mini-player save toggles now share one track-save label contract instead of hand-assembling saved-state titles/aria labels across surfaces.
    - Consolidated playback/review action labels so Listening Station row actions, mini-player review controls/tooltips, recommendation playback actions, and track queue buttons now share one play/queue/review label layer instead of repeating those action titles and aria copy across surfaces.
    - Consolidated playback CTA labels so play-now, play-match, and replay controls across recommendations, queue buttons, play-match buttons, and recently played now share one playback label helper instead of carrying parallel title/aria strings for the same playback flows.
    - Consolidated wishlist and add-source control labels so Listening Station record-wishlist/add-label actions, release wishlist buttons, recommendation want/add-source actions, and mini-player wishlist controls now share one release-action label contract instead of hand-assembling Discogs wishlist and add-source titles/button copy per surface.
    - Consolidated release-level Discogs link titles so Listening Station, recommendations, mini-player, and the release inspector now share one release-link title helper instead of carrying parallel “Open release on Discogs” strings.
    - Consolidated inbox/query source-mode branching so to-listen, played-reviewed, and wishlisted-record loaders now share one source-backed vs legacy-backed loader path instead of rebuilding the same label scope, active-label, and join-mode branching inline in each query.
    - Consolidated label-detail and dashboard label-metadata source-mode branching so the dashboard and label detail page now share one source-backed vs legacy-backed release/progress/count loader path instead of maintaining separate helper families for those query flows.
    - Consolidated query source-mode fallback execution so label detail, to-listen, played-reviewed, and wishlisted-record loaders now share one mode-based fallback helper plus one shared inbox-result loader instead of each re-wrapping the same source-backed vs legacy closures and result-finalization flow.
    - Consolidated hook result shaping so auth/account/source/library button hooks now share one `createActionRunResult` helper, and removed the now-unused `useAsyncTask` hook after moving its remaining callers onto `useAsyncTaskAction`.
    - Consolidated Listening Station release-scoped row mutations so optimistic wishlist toggles, rollback, affected-release confirmation, and add-source activation now use shared release-id mapping helpers instead of repeating row-by-row `releaseId` checks inline.
    - Consolidated playback feedback notices so shared playback button hooks, the Listening Station’s direct queue/play path, and the low-level queue/play client helpers now share one playback-notice layer instead of repeating queue/play/quota/no-match copy with drift.
    - Consolidated queue mutation client calls so the mini-player uses shared queue-scope, queue-dedupe, and queue-item removal helpers instead of embedding those request contracts inline.
    - Consolidated source worker execution so `/api/sources/next` and `/api/worker/process` share the same source-lock + single-step processing helper instead of maintaining parallel processing flows.
    - Consolidated OAuth route setup so Discogs and YouTube start/callback routes share the same request-origin, next-path, and login-redirect context helpers instead of repeating that provider flow wiring.
    - Consolidated saved-to-Discogs wishlist sync so the route now delegates target selection, Discogs writes, local confirmation, and feedback logging to shared wishlist-sync logic instead of hand-assembling that pipeline inline.
    - Consolidated Discogs wants sync route handling so wishlist sync status and auto-sync routes now use shared user-scoped service helpers instead of coupling route responses directly to auth-aware sync functions.
    - Consolidated labels-next compatibility state so it now shares the same source-status counts, next-source selection, and blocker messaging helpers as the primary sources-next flow.
    - Normalized settings key-test auth handling and extracted shared key-health logic so the route now participates in explicit protected-route `401` behavior instead of depending on deep auth throws.
    - Consolidated client-side library/review mutation calls so Listening Station and the mini-player share the same track, release wishlist, release reviewed, and add-source request helpers.
    - Consolidated recommendations-panel client mutations so recommendations now share the same track, release wishlist, add-source, and dismiss request helpers as the other client flows.
    - Consolidated Listening Station playback enqueue/error handling so it now uses the shared client playback helpers instead of carrying its own queue/error branch logic.
    - Consolidated client helper fetcher resolution so queue, source, wishlist, settings, release-data, label-action, and library-action helpers share one optional fetcher wrapper instead of each redefining it.
    - Consolidated source status/view shaping so the dashboard and source detail page share one source-view model for effective status, visible error, progress copy, and page-progress metadata.
    - Consolidated client playback/listening event dispatch so keyboard controls, queue controls, Listening Station, the mini-player, and shared playback helpers use one event helper layer instead of repeating raw `CustomEvent` wiring.
    - Consolidated client playback/listening event subscriptions so Listening Station and the mini-player now use shared typed playback-event subscriptions instead of re-parsing the same browser event payloads inline.
    - Consolidated client event-bus infrastructure so library, queue, source, and playback event helpers all share one generic dispatch/detail-read/subscribe layer instead of each reimplementing the same browser-event plumbing.
    - Consolidated protected mutation-route JSON response shaping so the main track/release/label/recommendation/youtube mutation routes share one API response helper instead of repeating the same `ok`/validation/not-found/conflict/error envelopes inline.
    - Consolidated protected mutation-route auth/rate-limit/body-parse scaffolding so the main POST mutation routes share one mutation prelude helper instead of each hand-assembling `requireRouteUserId`, rate limits, and schema-parse flow.
    - Extended the shared mutation prelude across the remaining queue, worker, track-match, and label mutation routes so most protected write paths now use the same auth/rate-limit/body-parse flow instead of route-local copies.
    - Consolidated numeric route-param parsing so public Discogs/Finder lookup routes and remaining source command routes now share one positive-int route helper instead of mixing ad hoc zod schemas and `Number(id)` checks.
    - Consolidated the remaining add-source mutation route onto the shared mutation prelude so source creation now uses the same auth/rate-limit/body-parse/error envelope flow as the other protected write endpoints.
    - Consolidated the remaining queue enqueue/advance write routes onto the shared mutation prelude so queue enqueue and queue-next POST now use the same auth/rate-limit/body-parse path as the rest of the protected mutation API.
    - Consolidated the remaining standalone client queue enqueue helper into the shared queue-action client module so playback queueing now shares one queue mutation/fetch contract instead of keeping a separate client queue wrapper.
    - Consolidated authenticated wishlist-sync/status route scaffolding so wishlist sync status/auto/manual/saved routes now share one auth/rate-limit/error wrapper instead of each hand-assembling the same route boilerplate.
    - Consolidated the remaining thin authenticated JSON routes so queue list, labels next, and settings key test now use the same shared auth/error route helper instead of repeating direct `requireRouteUserId` wrappers.
    - Consolidated legacy server-action source remediation flows so queue/retry/resume batch actions and the specific retry/refresh/pause-clear remediation actions now call the shared source batch/remediation helpers instead of duplicating the source update loops inside `app/actions.ts`.
    - Consolidated single-source process/retry/refresh flows so the dedicated label routes and legacy server actions now share one source-action service instead of duplicating owned-source lookup, processing kickoff, and best-effort metadata refresh behavior.
    - Consolidated settings integration disconnect handling so the active settings API/client path now uses one shared provider-disconnect service and the dead parallel server-action path has been removed.
    - Consolidated client-side source control/remediation helpers so single-source actions, batch control, and remediation now share one client source-action module instead of being split across parallel helper files.
    - Consolidated source status/activation mutation logic so the label status and active routes now share one source-status service instead of each carrying their own source lookup, transition, and legacy-schema fallback handling.
    - Consolidated source control/remediation action contracts so route schemas, client helpers, and UI components now share one source action vocabulary and one source-state mutation mapping instead of duplicating those enums and dispatch rules.
    - Consolidated client-side source mutation request parsing so the source-action module now shares one POST/JSON/error helper instead of repeating the same fetch/parse boilerplate across status, active, process, retry, refresh, control, and remediation actions.
    - Consolidated client JSON request helpers so settings, source intake, track-match selection, release-data lookup, and wishlist-sync helpers now share one fetch/POST/parse layer instead of each reimplementing their own `resolveClientFetcher` and JSON handling.
    - Consolidated the remaining queue/library/source-next client request helpers so queue mutations, queue list/advance loading, library mutations, and source-next polling now share the same client JSON request layer instead of keeping parallel fetch/POST/parse implementations.
    - Consolidated the remaining source-action client requests so label status/active/process/retry/refresh/delete plus source control/remediation now all run through the shared client JSON layer instead of keeping a separate source-mutation fetch/parse helper.
    - Consolidated source `[id]` mutation route preludes so delete, retry, refresh, status, and active now share one auth/rate-limit/source-id parsing helper instead of repeating that owned-source route setup in each file.
    - Consolidated public auth mutation routes so login, register, password-reset request, and password-reset complete now share one parse/run/400-response helper instead of each repeating the same public JSON mutation scaffold.
    - Consolidated single-source processing so the worker-process route now uses one shared owned-source processing service for not-found/inactive/paused/busy/error/success outcomes instead of keeping a separate lookup-and-run branch from the single-source action layer.
    - Consolidated source control/remediation execution so the control and remediation routes now delegate action branching and response payload planning to shared source-operation services instead of each route owning its own action-switch workflow.
    - Consolidated public release-data routes so Discogs release, Discogs label releases, and finder release lookups now share one numeric-param and error-mapping helper instead of each repeating the same public lookup route scaffold.
    - Consolidated queue enqueue error contracts so the enqueue route and client now share one set of queue-enqueue reason codes and one JSON-response reader, which also fixes explicit match failures drifting away from the client’s no-match handling.
    - Consolidated YouTube candidate search flow so `/api/youtube/search` and queue enqueue fallback now share one query-building/dedupe/scoring helper instead of maintaining separate search-result shaping logic.
    - Consolidated queue-next execution so the queue-next route’s GET selection and POST advance path now share one queue-next service instead of each route branch assembling its own selection/effect pipeline.
    - Consolidated track todo/reviewed update payloads so the tracks-todo route, release-reviewed route, and client library actions now share one track-update contract instead of each re-defining the same track response shape.
    - Consolidated release-library mutation contracts so release-reviewed and release-wishlist routes now share explicit response builders/types with the client library layer instead of keeping separate route payload shaping and client normalization logic.
    - Consolidated saved-to-Discogs wishlist sync summaries so the server sync service and client wishlist-sync layer now share one pure summary contract instead of normalizing the same result shape independently across server/client boundaries.
    - Consolidated source-intake add responses so dashboard source add and release-to-source add now share one source-intake contract instead of mixing `sourceId` and `labelId` payload shapes across server/client flows.
    - Consolidated single-source mutation payloads so source delete, retry, and refresh routes now share one source-mutation contract with the client source-actions layer instead of leaking legacy `labelId` response fields through those flows.
    - Consolidated source control/remediation response payloads so the batch source-operation routes and client source-actions now share one source-operations contract instead of normalizing the same `affected`/`kickedSourceId`/`nextPath` result shapes independently.
    - Consolidated account/session mutation payloads so login/register/reset/sign-out and provider disconnect now share one auth-mutation contract across server services, routes, and client helpers instead of repeating redirect/notice/provider response types in parallel.
    - Consolidated public release-data payloads so the Discogs release route, finder release route, finder service, and client release-data loaders now share one release-data contract instead of relying on route-local object assembly plus client-side `unknown` casts.
    - Consolidated queue mutation payloads so queue scope sync, queue dedupe, and queue-item removal now share one queue-mutation contract across routes and client queue-actions instead of each normalizing `removed`/`added`/`skipped`/`queueItemId` separately.
    - Consolidated library mutation payloads so the tracks-todo and recommendation-dismiss flows now share one library-mutation contract across routes and client library actions instead of using route-local payloads plus client-side fallback parsing.
    - Consolidated track-match selection payloads so the track-match route and client track-match actions now share one track-match contract instead of keeping another route-local `{ trackId, matchId }` response shape with client fallback parsing.
    - Consolidated source status/active/process responses so the remaining source-state routes and client source actions now share one source-state contract and one source-action service instead of mixing route-local payloads, raw worker responses, and duplicated dispatch logic.
    - Removed the extra client-library mutation wrapper so Listening Station and mini-player now call the shared library action service directly instead of routing through both `client-library-mutation-service` and `client-library-action-service` for the same mutations.
    - Removed the remaining library client wrappers so `use-library-mutation-actions`, Listening Station, and mini-player now call the shared library action service directly instead of routing through `client-library-actions` and `client-track-match-actions` for the same dispatching mutations.
    - Removed the remaining source client wrapper so `use-source-mutation-actions` and queue processor now call the shared source action service directly instead of routing through `client-label-actions` for the same source-state mutations.
    - Consolidated wishlist sync client mutations so the manual sync button, saved-to-Discogs button, status poller, and route-refresh bridge now share one wishlist-sync event contract instead of mixing hard reloads with independent polling.
    - Consolidated email auth/session mutations so login, register, password reset request/complete, and sign-out now share one auth service plus public JSON routes instead of mixing older server-action forms with the newer client/API mutation model.
    - Consolidated provider-start/connect entrypoints so Discogs, Google, and YouTube start/connect links now share one provider-path helper and reusable entrypoint components instead of hard-coded OAuth URLs scattered across onboarding, settings, login, and export surfaces.
    - Consolidated client-side confirmed/pending action handling so source remediation, source retry/refresh, source delete, integration disconnect, sign-out, and wishlist sync buttons now share one async action hook instead of each re-implementing confirm/pending/finally control flow.
    - Consolidated client-side async request state handling so source intake, source batch controls, API key testing, YouTube playlist export, and release link finder now share one async task hook instead of each hand-rolling loading/error/result state.
    - Consolidated auth-form submit/redirect handling so login, register, password reset request, and password reset completion now share one client auth-submit contract instead of each re-implementing pending/error/redirect flow.
    - Consolidated release-detail and small playback/source toggle actions so match selection, record wishlist, track listened/saved toggles, source activation, and play-this-match buttons now share the same async action/task hooks instead of bespoke one-off pending/error control logic.
    - Consolidated client-side mutation request helpers so auth, settings, library, source intake, track-match, and queue helper modules now share one POST/DELETE mutation wrapper instead of each repeating `requireClientJson` plus JSON-init boilerplate.
    - Consolidated client-side read/query JSON helpers so wishlist sync, settings key test, and release/finder data loaders now share one required/optional JSON response contract instead of each hand-rolling `fetchClientJson` plus `ok`/error extraction logic.
    - Consolidated client-side polling/query transport helpers so wishlist sync, source-next polling, queue-next fetch/advance, and queue-list loading now share one query helper layer instead of each wiring raw `fetchClientJson` + request init/options separately.
    - Consolidated source client mutation transport helpers so source status/active/process/retry/refresh/delete/control/remediation actions now share one source mutation wrapper instead of keeping a parallel request helper inside `client-label-actions`.
    - Consolidated compact playback action controls so track queue buttons, match-play buttons, recently played replay, and recommendation queue/play now share one keyed playback/quota/pending helper instead of each wrapping the same enqueue-and-handle-failure flow differently.
    - Consolidated keyed item-mutation controls so Recommendations and Listening Station release-level add-label/wishlist/dismiss/review actions now share one keyed async-action helper instead of each hand-rolling per-item pending and feedback flow.
    - Consolidated release-page and recommendation mutation controls so track todo, record wishlist, recommendation dismiss, add-source, and choose-match buttons now share one library/match mutation hook layer instead of mixing one-off `useAsyncAction` wrappers with panel-local keyed state.
    - Consolidated fetcher-aware library mutation wrappers so Listening Station and the mini-player now share one client library-mutation service for track todo, release wishlist, release reviewed, and add-source flows instead of each rebuilding transport wrappers over the same client actions.
    - Consolidated account/settings client mutations so login/register/reset/logout plus integration disconnect, key test, and YouTube export now share one account-action module instead of living in split auth-vs-settings client helper files.
    - Consolidated source control UI hooks so retry, refresh, delete, activate, batch sync control, and remediation buttons now share one source-mutation hook layer instead of mixing separate `useAsyncAction` and `useAsyncTask` wrappers over the same source client actions.
    - Consolidated source client-action normalization so retry, refresh, delete, control, and remediation helpers now share one source-action service for normalize-and-dispatch behavior instead of repeating that contract inside `client-label-actions`.
    - Consolidated account/settings action hooks so sign-out, integration disconnect, password-reset request, key testing, and playlist export now share one account-action hook layer instead of each re-wrapping the same client account actions with local pending/redirect/result logic.
    - Consolidated the remaining library mutation consumers so Listening Station and the mini-player now use the canonical client-library action layer instead of bypassing it through a parallel dispatching wrapper over the same track/release/add-source flows.
    - Consolidated auth submit hooks so login, register, reset request, reset complete, sign-out, integration disconnect, key test, and playlist export now all live under one account hook layer instead of splitting auth redirect submit flow into a separate hook module.
    - Removed the last account-action wrapper so login/register/reset/sign-out, integration disconnect, key test, and playlist export components now import the canonical client account module directly instead of routing through `use-account-actions`.
    - Removed the last source-action hook wrapper so retry, refresh, delete, activate, batch sync control, and remediation buttons now import the canonical client source action service directly instead of routing through `use-source-mutation-actions`.
    - Consolidated wishlist sync client hooks so manual sync, saved-to-Discogs sync, and the wishlist sync status pill now share one wishlist-sync hook layer instead of splitting pending/message/polling logic across separate components.
    - Removed the stale source-action hook wrapper so retry, refresh, delete, activate, batch sync control, and remediation buttons now import the canonical client source action service directly instead of routing through `use-source-actions`.
    - Removed the last wishlist-sync hook wrapper so manual sync, saved-to-Discogs sync, and the wishlist sync status pill now import the canonical client wishlist-sync module directly instead of routing through `use-wishlist-sync-actions`.
    - Removed the last library mutation hook wrapper so Listening Station, Recommendations, the mini-player, and release controls now import the canonical client library action service directly instead of routing through `use-library-mutation-actions`.
    - Removed the last playback hook wrapper so queue/play/replay controls now import the canonical client playback module directly instead of routing through `use-track-playback-action`.
    - Removed the reintroduced account hook wrapper so auth/session/settings controls now import the canonical client account module directly instead of routing through `use-account-actions`.
    - Removed the reintroduced source hook wrapper so source retry/refresh/delete/activate/control/remediation controls now import the canonical client source action service directly instead of routing through `use-source-actions`.
    - Consolidated source processor queue selection and safe-retry rules so the Queue Processor hook now uses shared source-processing helpers instead of carrying its own copy of queued/processing/error selection logic.
    - Consolidated source-next status presentation so the sync status panel now uses a shared source-sync view model/helper layer instead of interpreting source-next telemetry, deltas, anomaly labels, and top-failure aggregates inline.
    - Consolidated source processing loop state so Queue Processor now uses a dedicated `use-source-processor` hook instead of embedding its own retry/advance/status-update loop directly against the low-level source action service.
- Split source client hooks away from the low-level source action service so source retry/refresh/delete/activate/control/remediation UI now imports a dedicated `use-source-actions` layer while the underlying service stays transport-focused.
- Split account and wishlist-sync UI hooks away from their low-level client request modules so auth/settings controls and wishlist-sync controls now import dedicated hook layers while the underlying client modules stay transport-focused.
- Split library UI hooks away from the low-level library client request module so Recommendations, Listening Station, release controls, and mini-player client callbacks now import a dedicated `use-library-actions` layer while the underlying library module stays transport/dispatch-focused.
- Split playback UI hooks away from the low-level playback client module so queue/play/replay controls now import a dedicated `use-playback-actions` layer while the underlying playback module stays enqueue/failure-focused.
- Split source-next polling/subscription infrastructure away from source-sync view helpers so the source-next client surface now separates store/transport responsibilities from dashboard/view helper logic.
- Split the YouTube quota hook away from low-level session/event helpers so playback surfaces now import a dedicated `use-youtube-quota-state` hook while the underlying quota module stays storage/event-focused.
- Split the client route-refresh bridge’s generic mutation-event subscriptions away from its source-progress refresh logic so route refresh coordination and source-sync progress handling now live in separate client helpers.
- Consolidated Listening Station’s direct play-now flow onto the shared playback runner so inbox play behavior now shares the same pending/quota/request core as the other playback surfaces instead of hand-rolling its own path.
- Consolidated queue-change client helpers so queue-aware surfaces can use shared “any queue activity” subscriptions and queued-track-id fetch helpers instead of rebuilding enqueue-vs-mutation listeners and queue-list projection separately.
- Consolidated Listening Station queue row updates so optimistic enqueue marking and full queue resync now share queue-state helpers plus the shared queue-activity subscription instead of duplicating row-update logic inside the inbox component.
- Consolidated recommendation list state transitions so queue, dismiss, reviewed, save, and wishlist flows now share one recommendation-state helper instead of repeating local-vs-external removal and wishlist/saved update logic inline.
- Consolidated recommendation feed subscriptions and list state so queue, dismiss, todo, and wishlist event handling now lives in a dedicated recommendation-state hook instead of a second inline state machine inside the panel component.
- Consolidated recommendation row mutation wiring so in-library and outside-library recommendation cards now share a dedicated recommendation-action hook layer instead of each re-wrapping the same track/release mutation contracts inline.
- Consolidated source batch-action feedback so sync control and grouped remediation now share one feedback-summary layer and one inline error/message contract instead of remediation being a navigate-only special case.
- Consolidated wishlist-sync status polling so the status pill now uses a shared client store with event wakeups and adaptive polling instead of owning a one-off interval/event loop inside the hook.
- Consolidated source-sync status subscription/state bootstrapping so the source sync panel now uses a dedicated hook over the shared source-next store instead of owning its own live subscription and initial-state setup inline.
- Consolidated queued-track live membership so Listening Station now uses a shared queued-track-id client store with queue-event wakeups instead of owning its own queue-membership poll loop and throttling refs inline.
- Consolidated mini-player queue overlay lifecycle so the open-queue panel now uses a dedicated queue hook for fetch/dedupe/poll/remove/toggle behavior instead of keeping another inline live queue implementation inside the player component.
- Consolidated mini-player queue-activity reactions so queue-event refresh and listening-scope resync now flow through the dedicated queue hook instead of being split between the hook and a separate component effect.
- Consolidated playback mode client state so Listening Station and the mini-player now share one storage/dispatch/subscription hook instead of each managing playback-mode bootstrap and sync separately.
- Consolidated player-current client state so the mini-player now publishes through a shared current-player store and Listening Station now subscribes with emit-latest bootstrap instead of mixing raw player-current events with a separate request bootstrap effect.
- Consolidated listening-scope client state so Listening Station now publishes through a shared listening-scope store and the mini-player now subscribes with emit-latest bootstrap instead of splitting scope sync between raw event dispatch and component-local ref setup.
- Consolidated mini-player current-item state updates so current-event payload shaping plus saved/listened/wishlist current-state mutations now share one player-current-state helper instead of being repeated inline across toggle and subscription flows.
- Consolidated release finder link loading so the mini-player inspector and the manual release-link finder now share one cached client load/store hook instead of each running their own fetch/error/cache flow for the same release.
- Consolidated latest event-store plumbing so player-current and listening-scope client state now share one generic latest-event store helper instead of keeping duplicated publish/subscribe/emit-latest store logic in two modules.
- Consolidated client polling-store plumbing so source-next status, wishlist-sync status, and queued-track membership now share one generic singleton polling/wakeup helper instead of each carrying their own timer/in-flight/subscriber store implementation.
- Consolidated keyed client resource-store plumbing so release details and release finder links now share one generic cached client resource store instead of using two separate in-flight/cache/subscription patterns.
- Consolidated client store-consumer hooks so source sync status and wishlist sync status now use one external-store subscription helper over shared singleton stores instead of each keeping local `useState + subscribe` wrappers.
- Consolidated listening-scope live-state consumption so the mini-player now reads the shared latest-event store through external-store snapshots instead of remaining on a manual subscription effect.
- Consolidated Listening Station live queue/player overlays so inbox rows now derive player-current and queued-track state from shared store snapshots instead of mirroring those overlays through effect-driven local state.
- Consolidated mini-player playback command handling so browser/media-key controls and the internal playback event bus now share one dedicated playback-controls hook instead of keeping duplicate play/pause/next/prev/review wiring inline.
- Consolidated mini-player current-item event sync so player-current publishing, request-current replies, and current saved/wishlist mutation updates now share one dedicated hook instead of living as inline effects in the player component.
- Consolidated playback ownership lifecycle so mini-player visibility recovery, heartbeat renewal, storage handoff, and unload cleanup now share one dedicated hook instead of being scattered across inline effects.
- Consolidated remote playback controls so browser media keys and `navigator.mediaSession` handlers now share one dedicated hook instead of keeping duplicate play/pause/next/previous control paths inline in the mini-player.
- Consolidated mini-player primary controls so mobile and desktop previous/play-pause/review/release-review actions now share dedicated control components instead of duplicating the same playback/review buttons inline by layout.
- Consolidated Listening Station row actions so mobile queue controls and desktop row controls now share one dedicated row-action component layer for play/review/wishlist/save/add-label behavior instead of keeping duplicate breakpoint-specific button implementations inline.
- Consolidated Listening Station current-track quick actions so the mobile quick rail now uses a dedicated current-actions component layered on the same shared row-action controls instead of keeping another inline play/review/save/release-review implementation.
- Consolidated Listening Station mobile quick-rail shell so collapsed/expanded current-track chrome now lives in a dedicated quick-rail component instead of keeping another inline mobile-only render path inside the inbox component.
- Consolidated Listening Station queue-filter chrome so mobile and desktop queue filters now share one dedicated filter component for view/exclude/source/video/playback controls instead of maintaining separate inline button trees by breakpoint.
- Consolidated Listening Station selection/bulk controls so mobile and desktop bulk-action bars now share one dedicated component for select/clear/review/save flows instead of maintaining separate inline control trees by layout.
- Consolidated Listening Station mobile preset controls so commute/reset/compact/auto-next actions now share one dedicated mobile-controls layer instead of splitting the same mobile setting flows between the filter panel and quick rail.
- Consolidated Listening Station wishlist-source mode so the saved-vs-wishlisted filter strip and active summary now share one dedicated control component instead of splitting the same filter state across separate UI blocks.
- Consolidated Listening Station header shell so label navigation and the already-extracted control surface now share one dedicated header component instead of leaving the inbox client with another large inline top-of-surface render block.
- Consolidated Listening Station row-state derivation so the inbox row component now owns playback, wishlist, label-active, and row-summary shaping over the shared inbox-row type instead of rebuilding those props inline in the list shell.
- Consolidated Listening Station empty-state handling so the client and list shell now share one inbox-empty-state type and one rendered empty-state surface instead of duplicating the fallback card contract inline.
- Consolidated playback queue-item contracts so Listening Station, the mini-player, and compact playback buttons now share one playback-queue item type, and Listening Station’s optimistic play-now path now uses the same shared queue-item builder instead of carrying a parallel contract.
- Consolidated Listening Station direct playback flow so optimistic play-now dispatch, enqueue timeout policy, quota reset, and playback feedback now live in one dedicated inbox playback hook instead of another inline mutation lane in the client component.
- Consolidated Listening Station navigation flow so cursor clamping, current-row derivation, quick-rail visibility, next-needs-review targeting, and skip-plus-play-next now live in one dedicated inbox navigation hook instead of another inline control lane in the client component.
- Consolidated Listening Station library mutation flow so current-track, row-level, release-level, and bulk review/save/wishlist/add-label actions now live in one dedicated inbox library-actions hook instead of another dense inline mutation block in the client component.
- Consolidated Listening Station mobile preference persistence and live row/scope sync so session-backed quick-rail/compact/auto-next settings plus track-todo/queue/wishlist subscriptions and listening-scope publishing now live in dedicated inbox hooks instead of another large coordination block in the client component.
- Consolidated mini-player secondary controls so playback mode plus details/queue toggle actions now share one secondary-control layer across mobile “More controls” and desktop chrome instead of maintaining duplicate breakpoint-specific flows, and removed the stale dead mobile-only “More” block from the desktop strip.
- Consolidated mini-player auxiliary controls so save track, release wishlist, external YouTube/app links, playback mode, and details/queue toggles now share one aux-controls surface across mobile “More controls” and desktop chrome instead of maintaining separate secondary-action render paths by layout.
- Consolidated mini-player queue overlay so queue header, loading/error/empty states, and queue-item action rows now live in one dedicated overlay surface instead of another large inline render block inside the mini-player component.
- Consolidated mini-player now-playing playback panel so title/meta, Discogs release link, live BPM status/toggle, and seek timeline now live in one dedicated surface instead of another dense inline render block inside the mini-player component.
- Consolidated mini-player transport/review control rails so previous/play-pause and track/release review actions now share one control-rail surface across mobile and desktop instead of maintaining separate breakpoint-specific render blocks with inline tooltip wrapping.
- Restored the broader dashboard/source label metadata typing contract so shared query helpers again preserve the source fields consumed across dashboard, settings, welcome, and how-to-use pages.
- Consolidated mini-player library controls so current-track save and current-release wishlist buttons now share dedicated control components instead of keeping parallel inline render/tooltip/loading logic in the player component.
- Consolidated mini-player utility controls so open-on-YouTube, open-in-app, expand-details, and open-queue actions now share dedicated tooltip/icon control components, and removed the stale duplicate transport block that was still embedded in the desktop control section.
- Consolidated release-details presentation so the mini-player inspector and the release page now share one release-inspector panel over the same client resource/view layer instead of showing rich Discogs market/community details in only one surface.
- Consolidated the release-page link-finder flow into the shared release-inspector panel so the release page no longer keeps a second standalone “best links” surface over the same finder-links data.
- Consolidated the remaining release-page external-links flow into the shared release-inspector panel so the release page no longer renders a second standalone outbound-links section beneath the same release.
- Consolidated the release-page wishlist flow so the record wishlist action now lives once in the release header instead of being duplicated on every track row for the same release-level mutation.
- Consolidated release-page item mutation controls so release wishlist, track todo, and choose-match buttons now share one fixed-key release-item hook layer instead of keeping three parallel one-button wrappers over the same keyed library actions.
- Consolidated single-source button controls so retry, refresh, delete, and active-toggle buttons now share one source-button hook layer instead of each wrapping the same single-source action services independently.
- Consolidated batch source-operation buttons so sync control and remediation buttons now share one batch-source hook layer instead of separately re-wrapping the same batch action and next-path navigation behavior.
- Split auth form hooks from account/settings tool hooks so login/register/reset flows now import a dedicated auth hook module and sign-out/disconnect/key-test/export flows import a dedicated account-settings hook module instead of sharing one mixed account hook surface.
- Split single-source hooks from batch-source hooks so source retry/refresh/delete/active flows now import a dedicated single-source hook module and control/remediation flows import a dedicated batch-source hook module instead of sharing one mixed source-action surface.
- Split keyed library mutation hooks from fetcher-aware client callback hooks so interactive keyed flows still use `use-library-actions` while mini-player-style client callbacks now import a dedicated `use-library-client-actions` module instead of sharing one mixed library hook surface.
- Split the low-level playback runner from the keyed playback feedback hook so Listening Station now imports a dedicated playback-runner module while compact queue/play surfaces keep using `use-playback-actions` for the keyed feedback layer.
- Split wishlist sync mutation hooks from wishlist sync polling/status hooks so manual/saved sync buttons keep using `use-wishlist-sync-actions` while the status pill now imports a dedicated `use-wishlist-sync-status` module instead of sharing one mixed wishlist hook surface.
- Split low-level auth client mutations from account/settings client mutations so auth hooks now import a dedicated `client-auth-actions` module and settings/tool hooks import `client-account-settings-actions` instead of sharing one mixed account client surface.
    - Expanded `smoke:core` to use shared protected-route probe helpers instead of per-route duplicated checks.
    - Expanded `smoke:core` to cover public auth validation endpoints so middleware cannot silently block login/register/password-reset flows with a `401`.
    - Normalized protected API auth boundaries around a shared route helper so unauthenticated requests consistently return explicit `401` JSON responses.
    - Restored public API middleware access for auth and OAuth routes so login/register/password-reset plus Discogs/YouTube OAuth entrypoints are no longer intercepted by the generic protected-API guard.
    - Normalized the remaining queue API routes onto the shared route-auth helper so queue list/next/enqueue/scope match the rest of the protected API surface.
    - Normalized the remaining protected source routes so `sources-next` and `worker-process` now use the same explicit route-auth helper as the other protected JSON endpoints.
    - Extended smoke coverage to labels-next, wishlist sync status, settings key test, YouTube search, label status/active mutations, and queue-item deletion boundaries.
    - Verified the local release path with `yarn check` and `yarn build` on 2026-03-14.
    - Deployed commit `5a37a04` to Railway production on 2026-03-14.
    - Verified the live release path with `SMOKE_BASE_URL=https://digqueue-production.up.railway.app yarn release:verify:live` on 2026-03-14.
    - Hardened `release:verify:live` so it now waits for the latest Railway deployment to be promoted before falling through to base-URL readiness, removing the stale-revision smoke race from the release workflow.
  - Done when:
    - The release checklist is explicit and matches the current scripts and routes.
    - Duplicate-user and duplicate-queue hot paths are protected by schema-backed invariants instead of best-effort cleanup alone.
    - `yarn release:verify` passes locally and `yarn release:verify:live` passes against the target deploy after the production deploy is updated.

## Near-Term (P1)
- [ ] Final polish / low-risk cleanup
  - Completed in this pass:
    - Consolidated onboarding snapshot loading and setup-status card rendering so the dashboard, Welcome, How To Use, and Settings pages now share one server-side onboarding snapshot plus one status-card surface instead of drifting across four near-identical implementations.
    - Consolidated dashboard empty-state planning so Listening Station and Library views now use one shared planner with stronger next-step CTA coverage instead of a large inline block in `app/page.tsx`.
    - Consolidated highlighted account/connect CTA styling so auth submit buttons plus Discogs/YouTube connect entrypoints now share one reusable highlight-action layer instead of repeating hard-coded gold button classes across auth, connect, settings, and export surfaces.
    - Consolidated dashboard Discogs-required guidance so ingestion and wishlist-blocked states now share one guidance helper and one reusable notice component instead of repeating slightly different “connect Discogs” copy inline.
    - Consolidated settings and setup support shells so Settings section cards plus Discogs/YouTube status panels and the guided Discogs connect explainer now share reusable card/panel components instead of repeating the same bordered support markup across account surfaces.
    - Consolidated account/setup/dashboard feedback banners so login/reset notices, Discogs connect requirements, settings OAuth success/failure, and dashboard blocker/success notices now share one reusable feedback banner instead of repeating parallel success/error/warning alert markup.
    - Consolidated secondary action links so dashboard/library drill-ins, Discogs-required guidance, YouTube fix navigation, guided connect back-links, settings export/docs links, and existing external action links now share one generic action-link layer instead of keeping separate internal text-link/button markup beside a second external-link styling helper.
    - Consolidated auth/setup hero shells so login, reset-password, and guided Discogs connect now share one account-surface layout for the outer card, width handling, heading/intro spacing, and optional accent treatment instead of carrying parallel page-frame markup.
    - Consolidated dashboard/recommendation inset panels so source-intake info blocks, library activity stat cards, and recommendation view sections now share one inset-panel class helper plus one section-kicker label component instead of repeating the same low-emphasis panel chrome and uppercase heading styles inline.
    - Consolidated dashboard/library stat tiles so the main source/library metric cards plus the reviewed/history activity counters now share one stat-tile component instead of duplicating the icon-label-value layout and optional drill-in action wiring inline.
    - Consolidated neutral empty-state notes so dashboard source fallbacks, inbox/recommendation/recently-played empties, label/release empty results, and release-inspector no-pricing feedback now share one reusable empty-state note instead of scattering one-off muted fallback paragraphs and dashed empty cards.
    - Consolidated release-inspector sub-panels so artwork context, release/market snapshots, price guide, outbound links, and Bandcamp result sections now share one release-inspector panel helper plus one metric-cell helper instead of repeating the same dark card chrome and tiny market-stat cell markup inline.
    - Consolidated the remaining surface-level inset card wrappers so release track cards, listen inbox empty-state cards, and YouTube export controls now use the shared inset-panel wrapper instead of carrying the same `surface2` border/background shell inline.
    - Consolidated settings diagnostics/help flows so the Discogs quick-setup checklist and YouTube block-fix instructions now share one checklist component, the YouTube fix assistant uses the shared support-panel/action-link contract, and Settings now exposes live key diagnostics instead of leaving that tooling effectively hidden.
    - Consolidated hover-tooltip primitives so mini-player controls, listen-inbox icon actions, and the How To Use orientation page now share one tooltip wrapper plus one tooltip class helper instead of carrying separate hover/focus tooltip markup and sizing variants inline.
    - Consolidated guide-page support surfaces so the How To Use page now reuses shared warning banners, inset panels, and checklist lists instead of maintaining its own demo note box, muted helper cards, and handwritten numbered paragraphs.
    - Consolidated remaining warning feedback surfaces so the dashboard remediation summary, listen-inbox YouTube quota lockout, and Discogs-required callouts now reuse the shared feedback-banner contract instead of carrying bespoke amber alert markup.
    - Consolidated onboarding/docs guide cards so Welcome and How To Use now share one guide-card shell for icon/title/body sections, while Welcome also moves its numbered setup blocks onto the shared instruction-list contract.
    - Consolidated the remaining Welcome-page info cards so both highlight tiles and closing guidance cards now share one info-card shell instead of carrying two separate bordered marketing-card implementations in the same page.
    - Consolidated onboarding/docs hero sections so Welcome and How To Use now share one orientation-hero wrapper for title, description, CTA row, and supporting content instead of hand-rolling separate hero shells at the top of each page.
    - Consolidated link-driven button navigation so onboarding CTA rows, the Welcome guide shortcut, and Listen-page utility navigation/reset now share one button-link primitive instead of mixing nested `Link` + `Button` markup with handwritten bordered link buttons.
    - Consolidated authenticated page-header chrome so Settings and Listen now share one app-page-header wrapper for title, subtitle, spacing, and action placement instead of each hand-rolling the same top-of-page header structure.
    - Consolidated entity detail-page headers so label and release detail views now share one entity-detail-header wrapper for title, subtitle/meta text, and action badges instead of carrying parallel top-of-page header markup.
    - Consolidated media summary rows so label release cards and recommendation cards now reuse the shared media-action-row primitive instead of each maintaining separate artwork/title/meta header markup beside the queue overlay and recently played list.
    - Consolidated section card headers so listen, settings, label, and release cards now share one section-card-header wrapper for title, optional description, and optional actions instead of repeating direct CardHeader/CardTitle layouts inline.
    - Consolidated compact result rows so release match rows and external result links now share one compact-result-row primitive instead of maintaining separate bordered title/meta row markup across comparison surfaces.
    - Hardened the live release gate so `release:verify:live` now retries transient Railway deployment/service status probe failures before falling back to base-URL readiness, reducing noisy CLI flake during production verification.
  - Pick off any remaining copy/docs mismatches only if they show up during authenticated smoke verification or manual UX review.
  - Run authenticated smoke probes with `SMOKE_COOKIE` when a fresh production session is available.

## Later (P2)
## Next Execution Order
1. Run authenticated `release:verify:live` with `SMOKE_COOKIE` when a fresh production session is available.
2. Pick off any remaining UX/copy mismatches that appear in authenticated smoke or manual review.
3. Add more operational alerts only if the current smoke checks expose weak spots.
